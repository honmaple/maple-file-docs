# 访问频率限制

## 使用
首先打开红枫云盘，找到 **设置 -> 存储库**，选择已有存储或者添加新的存储，在设置好其它参数后选择 **更多设置**

![](/images/features/more-setting.png)
![](/images/features/ratelimit-01.png)

相关参数:
- **访问频率**：是否激活访问频率限制
- **次数限制**：单位时间内允许访问的次数(m)
- **时间限制**：单位时间，每 **n** 秒允许访问 **m** 次(n)
- **是否等待**：为否时将立即返回错误信息，激活时将等待限制结束